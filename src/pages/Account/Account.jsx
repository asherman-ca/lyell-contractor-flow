import React, { useState, useEffect } from 'react';

const Account = () => {
	const [reports, setReports] = useState();

	return (
		<div className='account'>
			Table of reports with important dates and forms / buttons to update
		</div>
	);
};

export default Account;
